{
  "name": "andre-ait-interactive",
  "version": "1.0.0",
  "private": true,
  "description": "Andre AI interactive session page — talking-head presenter with switching cinematic scenes.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
