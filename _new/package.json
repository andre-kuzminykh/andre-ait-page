{
  "name": "andre-ait-interactive",
  "version": "1.0.0",
  "private": true,
  "description": "Andre AI interactive session page — full-screen talking head that swaps a short per-section clip while content/buttons change.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.0"
  }
}
