# Слайд 37 лекции 3

Соседи: слайд 36 — Инженерия циклов; слайд 38 — Жизненный цикл ИИ-агента.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Полная архитектура <span class="text-solar">рабочего ИИ-агента</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Сквозная схема-«метро» из звеньев (узлы появляются по очереди, по линии бежит пунктир): Процесс
(AS-IS/TO-BE) → Спецификация агента → ИИ-операции (правило / вызов модели / агент) → Воркфлоу и данные →
Промт и контекст → Обвязка и среда исполнения. Под ней — рабочий цикл в момент выполнения: модель решает и
выбирает разрешённый инструмент → среда проверяет вызов и выполняет → результат меняет состояние → развилка
на три исхода: продолжить / завершить / передать человеку. Вывод: работающий агент — инженерная система, а не
модель.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-37.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. одной модели недостаточно
2. разбираем процесс (как сейчас и как должно), описываем агента (роль, цель, данные, полномочия, инструменты, ограничения, передача человеку, формат)
3. определяем операции: где правило, где вызов модели, где агент выбирает действия; проектируем воркфлоу, движение данных, промт и контекст
4. всё связывают обвязка и среда исполнения; модель выбирает разрешённый инструмент, среда проверяет и выполняет, результат меняет состояние
5. агент решает: продолжить, завершить или передать человеку; работающий агент — целая инженерная система

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 37. Полная архитектура рабочего ИИ-агента  (Блок 10. Инженерия работающего агента)

Чтобы ИИ реально работал в бизнесе, одной модели недостаточно.
Сначала разбираем процесс: как он работает сейчас и как должен работать после изменений.
Затем описываем агента: его роль, цель, данные, полномочия, инструменты, ограничения, передачу человеку и формат результата.
После этого определяем нужные интеллектуальные операции: где достаточно правила, где вызова модели, а где агент должен сам выбирать дальнейшие действия.
Дальше проектируем workflow, движение данных, промт и контекст.
Всё это связывают обвязка агента и среда исполнения.
Во время работы модель принимает решение и выбирает разрешённый инструмент, а среда исполнения проверяет вызов и выполняет действие.
Результат меняет состояние задачи, после чего агент решает: продолжать, завершить работу или передать её человеку.
То есть работающий агент — это не просто модель, а целая инженерная система.
