# Слайд 9 лекции 3

Соседи: слайд 8 — Компоненты ИИ-платформы; слайд 10 — Спецификация ИИ-агента.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">LLM-функция <span class="text-solar">или ИИ-агент</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Слева LLM-функция: письмо → один вызов → «возврат товара» (один вход, один вызов, один результат) —
прямая короткая стрелка. Справа ИИ-агент: цикл самостоятельного выбора действий — CRM (история заказов) →
замечает отсутствие документа → другой источник → анализ → решение; кольцо с узлами (a-step). Под агентом —
его состав семью плашками: цель, модель, промт, контекст, инструменты, цикл выполнения, условие завершения.
Внизу правило: решается одним вызовом — агент не нужен.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-09.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. один вход, один вызов, один результат — функция на основе LLM, а не агент (пример: категория письма «возврат товара»)
2. агентность — система сама выбирает следующие действия (пример: CRM → нет документа → другой источник → анализ → решение)
3. агент — цикл самостоятельного выбора действий для достижения цели в заданных границах
4. у агента: цель, модель, промт, контекст, инструменты, цикл выполнения, условие завершения
5. если задачу решает один вызов модели — агент не нужен; чем проще архитектура, тем легче контролировать и тестировать

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 9. LLM-функция или ИИ-агент  (Блок 3. Архитектура ИИ-агента)

Сегодня почти любой вызов языковой модели называют агентом, хотя это разные вещи. Например, мы передали письмо модели, попросили определить категорию и получили «возврат товара». Один вход, один вызов, один результат — это функция на основе LLM, а не агент.
Агентность появляется, когда система сама выбирает следующие действия. Например, ей нужна история заказов: она обращается к CRM, получает данные, замечает отсутствие документа, идет в другой источник, анализирует результат и потом принимает решение.
Агент имеет цикл самостоятельного выбора действий для достижения цели в заданных границах.
У агента есть: цель, модель для принятия решений, промт, контекст, инструменты, которые позволяют действовать, цикл выполнения и условие завершения.
Если задачу можно решить одним вызовом модели — агент не нужен. Чем проще архитектура, тем легче ее контролировать и тестировать.
