# Слайд 17 лекции 3

Соседи: слайд 16 — API и внешние системы; слайд 18 — Трансформация данных.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Потоки <span class="text-solar">данных</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Три шага воркфлоу, между которыми течёт JSON (моноширинные карточки данных). У каждого шага контракт:
вход → операция с параметрами → результат. Пример маппинга: шаг «клиент» вернул объект с customer.email —
это значение стрелкой подставляется в поле «получатель» шага «письмо» (подсветка поля, бегущая линия).
Рядом — серая карточка типичной ошибки: каждый блок работает, но следующий получил не то поле / объект /
тип. Внизу вопрос отладки крупно: откуда на этом шаге взялось каждое значение?

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-17.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. в автоматизации важны не ноды, а данные, которые проходят между ними
2. у каждого шага контракт: входные данные → операция с параметрами → результат, который становится входом следующего
3. данные обычно в JSON; нужное значение подставляют переменными и выражениями
4. пример: предыдущий шаг вернул объект клиента с полем customer.email, следующий подставляет его в получателя письма — это маппинг данных
5. частая ошибка: блоки работают, но следующий получает не то поле, объект или тип; главный вопрос отладки — откуда взялось каждое значение

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 17. Потоки данных  (Блок 5. Проектирование воркфлоу)

В автоматизации важны не ноды. Важно, какие данные проходят между ними.
Workflow — это не просто последовательность блоков на схеме, а движение данных между шагами.
У каждого шага есть контракт: входные данные → операция с параметрами и результат. Этот результат становится входом следующего шага.
Данные обычно передаются в JSON-формате. Чтобы следующий шаг использовал нужное значение, применяются переменные и выражения.
Например, предыдущий шаг вернул объект клиента с полем customer.email, а следующий должен отправить письмо. Мы подставляем это значение в поле получателя.
Такое связывание называется маппингом данных.
Здесь часто возникают ошибки: каждый блок работает правильно, но следующий получает не то поле, объект или тип данных.
Поэтому главный вопрос при отладке: откуда именно на этом шаге взялось каждое значение?
Если мы не понимаем движение данных, мы фактически не контролируем workflow.
