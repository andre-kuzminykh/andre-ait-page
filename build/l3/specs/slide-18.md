# Слайд 18 лекции 3

Соседи: слайд 17 — Потоки данных; слайд 19 — Маршрутизация данных.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Трансформация <span class="text-solar">данных</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Слева «сырые» данные, справа нормализованные, между ними конвейер операций (a-flow): переименовать
поля, изменить типы, нормализовать даты, очистить текст, удалить лишнее. Примеры до/после плашками: дата
строкой → дата; телефон с пробелами и разными кодами → единый формат; сумма «12 000,00 ₽» строкой → число.
Ниже две операции с массивами: разделить массив на записи / собрать сотни записей в один объект; и
фильтрация, сортировка, дубликаты, агрегация (сумма заказов, группировка по сегментам). Акцент-пример:
агент верно решил остановить платёж, но сумма пришла строкой — работа бесполезна.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-18.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. данные почти никогда не переходят между сервисами идеально: дата строкой, другой формат, телефон с пробелами и разными кодами
2. преобразование: переименовать поля, изменить типы, нормализовать даты, очистить текст, удалить лишнее
3. массив разделить на записи или собрать сотни записей в один объект; фильтрация, сортировка, удаление дубликатов, агрегация (сумма заказов, группировка по сегментам)
4. пример: агент верно решил остановить платёж, но сумма пришла строкой в неожиданном формате — работа бесполезна
5. преобразование данных — основа надёжной автоматизации

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 18. Трансформация данных  (Блок 5. Проектирование воркфлоу)

Даже идеальный ИИ-агент бесполезен, если следующий шаг получает данные в неправильном формате.
В реальных системах данные почти никогда не переходят между сервисами идеально. Где-то дата хранится строкой, где-то нужен другой формат, Или телефон приходит с пробелами или разными кодами страны.
Поэтому значительная часть workflow — это преобразование данных: переименовать поля, изменить типы, нормализовать даты, очистить текст или удалить лишнее.
Иногда массив нужно разделить на отдельные записи, а иногда наоборот — собрать сотни записей в один объект. Сюда же относятся фильтрация, сортировка, удаление дубликатов и агрегация — например, сумма заказов или группировка клиентов по сегментам.
Именно здесь ломается большое количество реальных процессов.
Агент может правильно решить остановить платеж, но если следующий шаг получил сумму строкой в неожиданном формате, вся работа становится бесполезной.
Поэтому преобразование данных — основа надежной автоматизации.
