# Слайд 7 лекции 3

Соседи: слайд 6 — Четыре слоя ИИ-платформы; слайд 8 — Компоненты ИИ-платформы.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Среда исполнения <span class="text-solar">агента</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Слева «один вызов модели»: запрос → ответ (две плашки, коротко). Справа — длинная временная лента
реального агента: вызов модели → внешний сервис → сохранение → ожидание события (пауза: часы или дни,
иконка часов, лента прерывается пунктиром) → продолжение с нужного места → снова модель. Над лентой — что
делает среда исполнения: передача данных между шагами, хранение состояния, вызов инструментов, учётные
записи, обработка сбоев. Пример с договором: отправил на подтверждение → ответ завтра → остановить,
сохранить, продолжить. Вывод: модель решает, что делать; среда превращает решение в контролируемый процесс.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-07.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. один вызов модели прост; реальный агент несколько раз вызывает модель, обращается к сервисам, сохраняет результат, ждёт событие и продолжает через часы или дни
2. среда исполнения: передача данных между шагами, хранение состояния, вызов инструментов, доступ к учётным записям, обработка сбоев
3. пример: договор на подтверждение, ответ придёт завтра — остановить, сохранить состояние, продолжить с нужного места
4. это инженерия исполнения; модель решает, что делать, среда превращает решение в контролируемый процесс

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 7. Среда исполнения агента  (Блок 2. Платформа и среда исполнения)

Почему ИИ-агенты отлично выглядят на демке, а в проде начинают разваливаться? Часто причина — отсутствие полноценной среды исполнения.
Один вызов модели прост: передали запрос — получили ответ. Но реальный агент может несколько раз вызвать модель, обратиться к внешнему сервису, сохранить результат, дождаться события и продолжить работу через часы или дни.
Среда исполнения отвечает за передачу данных между шагами, хранение состояния, вызов инструментов, доступ к учетным записям и обработку сбоев.
Например, агент отправил договор на подтверждение. Если ответ придет завтра — значит, процесс нужно остановить, сохранить состояние и продолжить с нужного места.
Это уже не интеллект модели, а инженерия исполнения.
Модель решает, что делать, а среда исполнения превращает решение в контролируемый процесс. Здесь заканчивается демо и начинается полноценная работающая система.
