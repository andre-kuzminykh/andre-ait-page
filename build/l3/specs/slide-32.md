# Слайд 32 лекции 3

Соседи: слайд 31 — Права ИИ-агентов; слайд 33 — Цикл работы ИИ-агента.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Контроль <span class="text-solar">и восстановление</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Длинный процесс из десяти шагов-точек: восемь выполнены, на девятом сбой (молния); контрольные точки
отмечены флажками; стрелка восстановления возвращает к последней надёжной точке, а не к началу (перечёркнутое
«начать заново»). Над лентой — четыре механизма карточками с иконками: Проверка (структура, поля, параметры,
разрешённость, результат системы — до и после), Трассировка (контекст, версия, инструмент, параметры, ответ
системы), Контрольные точки, Восстановление. Итоговая строка: предотвращает · объясняет · сохраняет ·
продолжает.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-32.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. что делать, если агент сломался на девятом шаге из десяти: надёжный агент умеет восстанавливаться
2. проверка на критических этапах: структура ответа, обязательные поля, параметры, разрешённость, результат внешней системы — до и после действия
3. трассировка: контекст, версия, выбранный инструмент, параметры, ответ внешней системы
4. контрольные точки: восемь дорогих шагов не повторять заново; восстановление — продолжить с последней надёжной точки
5. проверка предотвращает, трассировка объясняет, контрольные точки сохраняют прогресс, восстановление продолжает

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 32. Контроль и восстановление  (Блок 8. Инженерия обвязки)

Что делать, если агент сломался на девятом шаге из десяти?
Надёжный агент должен уметь не только работать, но и восстанавливаться после ошибок.
Первый механизм — проверка. На критических этапах система проверяет структуру ответа, обязательные поля, параметры инструментов, разрешённость действий и результат внешней системы. Причём проверки нужны и до действия, и после него.
Второй — трассировка. Мы сохраняем, какой контекст получила модель, какая версия использовалась, какой инструмент был выбран, с какими параметрами и что вернула внешняя система.
Для длинных процессов нужны контрольные точки. Если уже выполнено восемь дорогих шагов и на следующем произошёл сбой, начинать всё заново не нужно — состояние периодически сохраняется.
А восстановление позволяет продолжить с последней надежной точки.
Итого: проверка предотвращает ошибки, трассировка помогает понять причины, контрольные точки сохраняют прогресс, а восстановление позволяет безопасно продолжить работу.
