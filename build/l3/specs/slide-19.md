# Слайд 19 лекции 3

Соседи: слайд 18 — Трансформация данных; слайд 20 — Управление воркфлоу.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Маршрутизация <span class="text-solar">данных</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Три мини-схемы рядом одинаковой высоты с живыми анимациями: Ветвление (условие да/нет и выбор одного
из нескольких — бежит по одной ветке), Разветвление (один результат уходит сразу в три стороны: обновить CRM,
задача на доставку, уведомление клиенту — пунктир бежит по всем трём), Объединение (три ветки сходятся в
одну точку). Под ними итоговая строка: ветвление выбирает маршрут · разветвление запускает несколько
направлений · объединение соединяет результаты. Нюанс-плашка: параллельные ветки на схеме не всегда
выполняются одновременно — зависит от среды исполнения.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-19.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. как только процесс не прямой, появляется маршрутизация — куда пойдут данные дальше
2. условие: если критерий выполнен — одна ветка, иначе другая; несколько вариантов — выбор одного из нескольких
3. разветвление — результат сразу в несколько направлений (обновить CRM, задача на доставку, уведомление клиенту)
4. объединение собирает результаты веток вместе
5. нюанс: параллельные ветки на схеме не всегда выполняются одновременно — зависит от среды исполнения

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 19. Маршрутизация данных  (Блок 5. Проектирование воркфлоу)

Автоматизация — это не всегда цепочка шагов. Иногда она начинает ветвиться.
Как только процесс перестает быть прямым, появляется маршрутизация — нужно решить, куда данные пойдут дальше.
Самый простой вариант — условие: если критерий выполнен, идем по одной ветке, если нет — по другой. Если вариантов несколько, можно выбрать один из нескольких.
Но важно не путать это с разветвлением на несколько параллельных действий. Здесь система не выбирает одну ветку, а отправляет результат сразу в несколько направлений.
Например, после обработки заказа нужно одновременно обновить CRM, создать задачу для доставки и отправить уведомление клиенту.
Если потом результаты нужно снова собрать вместе, используется объединение.
Итого: ветвление выбирает маршрут, разветвлением запускает несколько направлений, а объединение соединяет результаты.
И ещё нюанс: параллельные ветки на схеме не всегда выполняются одновременно — это зависит от среды исполнения.
