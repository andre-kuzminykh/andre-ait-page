# Слайд 15 лекции 3

Соседи: слайд 14 — Вызов функций и схема инструментов; слайд 16 — API и внешние системы.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Выбор инструментов <span class="text-solar">и динамические аргументы</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Слева серая огромная функция «управлять CRM» (перечёркнута, туманная). Справа — четыре узких навыка
карточками: найти клиента, история заказов, создать задачу, список транзакций. Под ними два шага агента:
1) выбирает инструмент из разрешённых (одна карточка подсвечивается), 2) формирует параметры из контекста —
пример вызова «список транзакций» с полями клиент / период, значения заполняются по очереди (a-in).
Сохранить выверенную владельцем прошлую раскладку «этапов», где уместно. Внизу баланс: достаточно узкий,
чтобы ограничить опасную свободу, достаточно понятный, чтобы агент знал, когда его использовать.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-15.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. чем универсальнее инструмент, тем хуже он работает: одна огромная функция «управлять CRM» непонятна агенту
2. надёжнее узкие навыки: найти клиента, получить историю заказов, создать задачу, получить список транзакций
3. агент решает две задачи: выбирает инструмент из разрешённых и формирует параметры из текущего контекста (клиент, период)
4. пространство действий задаём заранее, выбор и аргументы формируются динамически
5. инструмент достаточно узкий, чтобы ограничить опасную свободу, и понятный, чтобы агент знал, когда его использовать

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 15. Выбор инструментов и динамические аргументы  (Блок 4. ИИ-операции и инструменты)

Чем универсальнее инструмент вы даёте агенту, тем хуже он обычно работает.
Надёжность агента зависит от качества его инструментов. Если дать агенту одну огромную функцию вроде «управлять CRM», ему будет сложно понять его назначение.
Надёжнее давать узкие навыки: найти клиента, получить историю заказов, создать задачу или получить список транзакций.
Во время работы агент решает две задачи. Сначала выбирает подходящий инструмент из разрешённых. Затем формирует его параметры на основе текущего контекста.
Например, для истории транзакций он сам определяет клиента, период и другие необходимые параметры.
То есть пространство действий мы задаём заранее, а конкретный выбор и аргументы формируются динамически.
Инструмент должен быть достаточно узким, чтобы ограничивать опасную свободу, и достаточно понятным, чтобы агент надёжно понимал, когда его использовать.
Чем точнее мы определили список инструментов, тем предсказуемее вся система.
