# Слайд 34 лекции 3

Соседи: слайд 33 — Цикл работы ИИ-агента; слайд 35 — Метрики для тестирования.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Как правильно тестировать <span class="text-solar">ИИ-агента</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Слева набор тестовых сценариев шестью плашками-карточками: обычные случаи, отсутствующие данные,
неоднозначные запросы, редкие ситуации, ошибки внешних сервисов, правильный ответ — передать человеку; у
каждого «ожидаемое поведение». Справа — проверка всего пути выполнения как трасса-лента с галочками:
понял ситуацию → выбрал маршрут → нужный инструмент → корректные параметры → только разрешённые действия →
финальный ответ. Акцент-предупреждение: прекрасное объяснение и одновременно запрещённая операция.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-34.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. агент может отвечать идеально и работать неправильно; несколько примеров ничего не говорят о надёжности
2. набор сценариев: обычные, отсутствующие данные, неоднозначные запросы, редкие ситуации, ошибки внешних сервисов, случаи, где правильно передать человеку
3. для каждого теста заранее ожидаемое поведение
4. проверяем путь: понял ли ситуацию, выбрал маршрут, нужный инструмент, корректные параметры, только разрешённые действия
5. агент может написать прекрасное объяснение и совершить запрещённую операцию; тестируется весь путь, а не последняя фраза

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 34. Как правильно тестировать ИИ-агента  (Блок 9. Тестирование и улучшение агентов)

Агент может отвечать идеально — и при этом работать неправильно.
Несколько примеров почти ничего не говорят о его надёжности. На реальных пользователях быстро появляются ситуации, которых никто не проверял.
Поэтому нужен полноценный набор тестовых сценариев: обычные случаи, отсутствующие данные, неоднозначные запросы, редкие ситуации, ошибки внешних сервисов и случаи, когда правильное решение — передать задачу человеку.
Для каждого теста заранее задаём ожидаемое поведение.
И проверяем не только финальный ответ. Важно, правильно ли агент понял ситуацию, выбрал маршрут, использовал нужный инструмент, корректно сформировал параметры и выполнил только разрешённые действия.
Потому что агент может написать прекрасное объяснение и одновременно совершить запрещённую операцию.
Поэтому тестируется весь путь выполнения агента, а не только последняя фраза модели.
Чем лучше тестовый набор отражает реальные ситуации, тем точнее мы понимаем настоящую надёжность системы.
