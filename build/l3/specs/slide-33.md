# Слайд 33 лекции 3

Соседи: слайд 32 — Контроль и восстановление; слайд 34 — Как правильно тестировать ИИ-агента.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Цикл работы <span class="text-solar">ИИ-агента</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

КРУГ: Наблюдает → Решает → Действует → снова наблюдает, в центре «агент», пример по кругу (данных не
хватает → запросил историю транзакций → состояние изменилось → новое решение). Справа два блока «когда
остановиться»: Условие завершения (обязательные данные, подзадачи, результат прошёл проверку — галочки) и
Технические лимиты (итерации, вызовы инструментов, время, стоимость — шкалы-индикаторы). Серый намёк на
бесконечный цикл, если лимитов нет. Вывод: хороший агент достигает цели с приемлемыми затратами и надёжно
завершает работу.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-33.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. агент работает циклом: наблюдает → принимает решение → действует → снова наблюдает
2. пример: данных не хватает — запросил историю транзакций, состояние изменилось, следующее решение на новых данных; маршрут заранее знать необязательно
3. условие завершения: получены обязательные данные, выполнены подзадачи, итог прошёл проверку
4. технические лимиты: число итераций и вызовов инструментов, время и стоимость — иначе почти бесконечный цикл
5. хороший агент достигает цели с приемлемыми затратами и надёжно завершает работу

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 33. Цикл работы ИИ-агента  (Блок 9. Тестирование и улучшение агентов)

ИИ-агент может сам выбирать следующие шаги. Но кто скажет ему, когда остановиться?
Настоящий агент часто работает циклом: наблюдает → принимает решение → действует → снова наблюдает.
Например, агент проверяет клиента и понимает, что данных не хватает. Он запрашивает историю транзакций, получает результат, состояние задачи меняется — и следующее решение принимается уже на новых данных.
Так цикл может повторяться несколько раз, поэтому полный маршрут заранее знать необязательно.
Но нужно заранее определить условие завершения: получены все обязательные данные, выполнены нужные подзадачи и итоговый результат прошёл проверку.
Кроме этого нужны технические лимиты: максимальное число итераций и вызовов инструментов, ограничения по времени и стоимости.
Иначе ошибка в логике может запустить практически бесконечный цикл.
Поэтому хороший агент — не тот, который работает бесконечно, а тот, который достигает цели с приемлемыми затратами и надёжно завершает работу.
