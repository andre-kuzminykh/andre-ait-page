# Слайд 1 лекции 3

Соседи: слайд 0 — От бизнес-процесса к автоматизации; слайд 2 — Триггер Обработка Действие.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">AS-IS <span class="text-solar">и TO-BE</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Две дорожки процесса одна над другой. AS-IS (серая): длинная цепочка ручных шагов с возвратами —
документ пересылают, данные вручную в таблицу, проверяют, снова вводят в другую систему; возврат показан
петлёй. TO-BE (тёмная): короткая цепочка — бизнес-результат → лишнее убрано → правило / человек / ИИ на своих
местах. Между ними — порядок как ступени: Правильный процесс → Архитектура → Автоматизация (a-step по очереди).
Предупреждение «автоматизировать хаос — ускорить хаос».

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-01.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. AS-IS — как процесс работает сейчас, TO-BE — как должен работать после изменений
2. пример: пересылка документа, ручной перенос в таблицу, проверка, повторный ввод в другую систему
3. TO-BE не строится по принципу «вместо человека теперь ИИ»: сначала бизнес-результат, убираем лишнее, потом решаем, где правило, где человек, где ИИ
4. порядок: сначала правильный процесс, затем архитектура, и только потом автоматизация — иначе автоматизируем хаос

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 1. AS-IS и TO-BE  (Блок 1. От бизнес-процесса к автоматизации)

Одна из главных ошибок автоматизации — автоматизировать процесс, который изначально устроен неправильно. Так можно ускорить работу и в итоге сделать компанию только хуже.
Например, документ пересылают между сотрудниками, данные вручную переносят в таблицу, проверяют, а затем снова вводят в другую систему. Если просто автоматизировать эти шаги, мы лишь ускорим неэффективную систему.
Поэтому различают AS-IS — как процесс работает сейчас, и TO-BE — как он должен работать после изменений.
TO-BE нельзя строить по принципу «вместо человека теперь AI. Сначала определяем бизнес-результат, убираем лишние действия. Затем решаем, где достаточно простого правила, где остается человек и где действительно требуется искусственный интеллект.
Сначала правильный процесс, затем архитектура и только потом автоматизация. Иначе мы автоматизируем хаос. При правильном порядке AI-агенты действительно меняют работу бизнеса.
