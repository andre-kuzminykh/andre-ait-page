# Слайд 2 лекции 3

Соседи: слайд 1 — AS-IS и TO-BE; слайд 3 — Правила или ИИ-автоматизация.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Триггер <i class="ph-bold ph-arrow-right text-black/25"></i> Обработка <i class="ph-bold ph-arrow-right text-black/25"></i> <span class="text-solar">Действие</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

Горизонтальный конвейер из трёх крупных узлов с бегущим пунктиром между ними. Под «Триггером» —
три способа запуска (вручную / по расписанию / по событию: письмо, заказ, статус). Под «Обработкой» —
проверка, фильтрация, расчёты, формат, бизнес-правила и отдельно выделенная маленькая зона «ИИ — где нужен
смысл». Под «Действием» — запись в системе, сообщение, документ, вызов сервиса. Сквозной пример письма
пунктирной строкой по трём узлам: письмо из почты → правило проверяет отправителя → модель определяет смысл →
система выбирает маршрут. Внизу три вопроса: что запускает, что внутри, что должно измениться.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-02.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. три части любой автоматизации: триггер, обработка, действие
2. запуск: вручную, по расписанию, по событию во внешней системе (письмо, заказ, статус)
3. обработка: проверка, фильтрация, расчёты, преобразование формата, бизнес-правила; ИИ — только где нужна смысловая интерпретация
4. пример письма: почта → правило проверяет отправителя → модель определяет смысл → система выбирает маршрут
5. анализ — ещё не автоматизация: результат — реальное изменение (запись, сообщение, документ, вызов сервиса)
6. три вопроса: что запускает процесс, что происходит внутри, что должно измениться

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 2. Триггер → Обработка → Действие  (Блок 1. От бизнес-процесса к автоматизации)

Любую автоматизацию можно разобрать на три части: Триггер, Обработка и действие.
Сначала процесс должен чем-то запуститься: вручную пользователем, по расписанию или по событию во внешней системе — например, пришло письмо, появился заказ, или изменился статус в системе.
Дальше начинается обработка. Система проверяет и фильтрует данные, выполняет расчеты, преобразует формат и применяет обычные бизнес-правила. И только там, где нужна смысловая интерпретация, подключается AI.
Например, письмо получаем из почты, правилом проверяем отправителя, модель определяет смысл обращения, а система выбирает дальнейший маршрут.
И наконец — действие. Анализ сам по себе еще не автоматизация. Результатом должно стать реальное изменение: запись в системе, сообщение, новый документ или вызов другого сервиса.
Поэтому всегда задаем три вопроса: что запускает процесс, что происходит внутри и что должно измениться в результате. Тогда архитектура автоматизации становится понятной.
