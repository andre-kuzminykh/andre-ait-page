# Слайд 30 лекции 3

Соседи: слайд 29 — Что такое обвязка ИИ-агентов; слайд 31 — Права ИИ-агентов.

## Заголовок (вставить дословно — это заголовок владельца из его текста)

```html
<h2 class="text-xl sm:text-3xl md:text-5xl font-black mb-3 md:mb-5 text-center text-black">Из чего состоит обвязка <span class="text-solar">ИИ-агента</span></h2>
```

## Подзаголовок — пишешь сам (правила)

Одна строка на ПК, не длиннее 52 знаков, мысль из текста владельца, которая
ДОПОЛНЯЕТ заголовок и НЕ повторяет полосу вывода внизу слайда (владелец
вычёркивает повторы). Классы строго такие:
`<p class="text-center text-black/60 mb-4 md:mb-7 text-[10px] md:text-lg font-medium max-w-3xl mx-auto">…</p>`

## Направление: как сделать инфографику лучше прежней

КРУГ-схема: в центре Модель, по кольцу шесть частей (контекст, инструменты, состояние, память, среда
исполнения, — и одна дуга «эксплуатация»), снаружи второе кольцо/пояс «для реальной системы»: права доступа ·
лимиты (вызовы, время, стоимость) · проверки входов и результатов · трассировка · восстановление после
сбоев. Можно как два концентрических кольца (внутреннее вращается медленно). Внизу маленькая плашка: это не
обязательно один продукт — платформа или несколько сервисов, главное — все механизмы.

Прошлая версия этого слайда (выверена владельцем, бери удачное): `build/l3/base/slide-30.html`.
История правок владельца (ищи свою тему по цитатам): `build/l3/owner-notes.md`.

## Главное из текста — обязано быть на слайде (не всё подряд, но это — да)

1. чем больше свободы у агента, тем лучше должна быть обвязка
2. в центре модель; рядом контекст и инструменты; состояние хранит прогресс, память связывает взаимодействия, среда исполнения выполняет шаги
3. для реальной системы: права доступа, лимиты на вызовы, время и стоимость, проверки входов и результатов, трассировка, восстановление после сбоев
4. это не обязательно один продукт: всё может дать платформа или несколько сервисов — главное наличие всех механизмов

## Текст для озвучки (первоисточник, дословно — слайд должен ему соответствовать)

## 30. Из чего состоит обвязка ИИ-агента  (Блок 8. Инженерия обвязки)

Чем больше свободы у ИИ-агента, тем лучше должна быть обвязка вокруг него.
Обвязка агента — это набор механизмов, которые делают его управляемым.
В центре находится модель — она отвечает за интеллектуальную часть. Рядом — контекст, на основании которого принимается решение, и инструменты, через которые агент получает данные и действует во внешнем мире.
Состояние хранит текущий прогресс, память связывает разные взаимодействия, а среда исполнения отвечает за выполнение шагов, продолжение работы и передачу результатов.
Но для реальной системы этого мало.
Нужны права доступа, лимиты на вызовы инструментов, время и стоимость, проверки входов и результатов, трассировка и восстановление после сбоев.
Всё это вместе и образует обвязку агента.
Причём это не обязательно один продукт: в одном проекте всё может давать платформа, в другом — несколько сервисов.
Главное — наличие всех этих механизмов.
