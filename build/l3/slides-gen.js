export const meta = {
  name: 'lecture3-slides',
  description: 'Лекция 3: улучшение слайдов под новый текст — автор, придирчивый критик, ревизия в обеих темах',
  phases: [{ title: 'Вёрстка' }, { title: 'Приёмка' }, { title: 'Ревизия' }],
}

const IDS = (args && args.slides) || []
const pad = (n) => (n < 10 ? '0' + n : '' + n)
const R = '/home/user/andre-ait-page'

const CANON = `ГЛАЗА ВЛАДЕЛЬЦА — чеклист, по которому он принимает слайд (из его прошлых правок):
1. Масштаб: слайд не мелкий. probe.py печатает «ПК масштаб» — норма ≥ 0.95 (лучше 1.0–1.3).
   Меньше — слайд переполнен: убрать лишнее, сократить тексты, уменьшить число блоков.
   И не пусто: содержимое заполняет холст, карточки и иконки крупные.
2. Ничего не наслаивается, ничего не вылезает за карточку и экран, слова не рвутся посреди,
   подгонщик не ужимает текст (probe ловит «подгонщик ужал кегль» и «текст шире карточки»).
3. Где можно в одну строку — в одну строку: подзаголовок, плашки, короткие заголовки карточек.
   Где две строки — перенос по смыслу (явный « <br>» перед второй частью фразы),
   висячее одно слово на последней строке — брак. Максимум три строки в блоке.
4. Всё по центру: иконки и слова в карточках центрированы; ряд карточек — одной высоты
   и ширины; левый и правый блоки — на одном уровне; иконка прижата к своему слову.
5. Минимализм: никаких надписей-рубрик и подписей-определений НАД блоками — блок говорит сам
   за себя, заголовок карточки/колонки — 1–3 слова; никаких порядковых цифр «01/02» в элементах;
   никакого жирного в плашках и пояснениях.
6. Однородность: если у одной из параллельных карточек есть галочки/иконки — они у всех.
7. Круг — ровный круг (aspect-square в обёртке), подписи узлов короткие, не вылезают.
8. Полоса вывода — внизу, с воздухом над ней; блоки не стоят вплотную; вывод НЕ повторяет
   заголовок и подзаголовок другими словами.
9. Телефон 376×844: читаемо (кегль ≥ 10px), ничего не спрятано, то же содержание.
10. Слова владельца: воркфлоу, промт (не «промпт»), обвязка, ИИ-агент, запуск (не «выпуск»),
    оркестрация; латиница — только белый список контракта.
11. После ЛЮБОЙ правки масштаб подгонщика меняется и строки переезжают — финальный probe.py
    и взгляд на кадры обязательны ПОСЛЕ последней правки, а не до неё.
12. ВЛАДЕЛЕЦ ПРОСИЛ: «чтобы было круто и всё анимированно», «инфографики различные», «чтобы по тексту всё
    совпадало». Живая схема лучше мёртвой: кольца вращаются (a-orbit), линии бегут (a-flow/a-flow-y),
    узлы загораются по очереди (a-step), элементы появляются (a-in…a-in-6), акцент дышит (a-pulse).
    Образцы высокого уровня: ${R}/build/l6/out/slide-09.html, -17, -20, -23, -24, -37.`

const AUTHOR = (id) => `Ты улучшаешь ОДИН слайд лекции 3 курса («От бизнес-процесса к работающему ИИ-агенту»).
Текст озвучки изменился, заголовки теперь ДОСЛОВНО из текста владельца, а инфографика должна стать
красивее и живее прежней. Владелец очень придирчив: к масштабу, переносам, выравниванию, минимализму
и к соответствию текста. Твоя цель — чтобы ему не пришлось ничего править.

СНАЧАЛА ПРОЧИТАЙ ЦЕЛИКОМ:
1. ${R}/build/l3/CONTRACT.md — контракт и канон владельца. Каждое правило обязательно.
2. ${R}/build/l3/specs/slide-${pad(id)}.md — задание: заголовок (дословно!), правила подзаголовка,
   направление улучшения, «главное» (обязано быть на слайде) и текст озвучки.
3. Прошлую версию слайда ${R}/build/l3/base/slide-${pad(id)}.html и историю правок владельца
   ${R}/build/l3/owner-notes.md (найди свою тему по цитатам — выверенное не откатывать).
4. Образцы в ${R}/automation/4/index.html (Read с offset/limit): 2049-2081 (обложка), 2135-2216
   (веер), 2217-2302 (кольцо с агентом), 5622-5700 (фиолетовый финал с кнопкой теста), и принятые
   слайды лекции 6: ${R}/build/l6/out/slide-09.html, slide-20.html, slide-24.html.

ЧТО СДЕЛАТЬ:
1. Собери слайд и запиши Write в ${R}/build/l3/out/slide-${pad(id)}.html (только разметка,
   корневой тег id="slide-${id}"; прошлую версию в out можно целиком заменить). Направление
   задания — ориентир, а не закон: если элемент перегружает слайд — упрости, но «главное» не теряй.
   Слайд — инфографика с анимацией, а не список карточек.
2. Линтер, пока не «чисто»: cd ${R} && python3 build/l3/lint.py build/l3/out/slide-${pad(id)}.html
3. Замер обеих форм (≈20 с): cd ${R} && python3 build/l3/probe.py ${id}
   Печатает проблемы (✗), висячие слова, блоки 3+ строк; кадры ${R}/build/l3/frames/slide-${pad(id)}-pc.png
   и -mob.png (тёмная тема сайта по умолчанию — bg-white там тёмно-серый, это норма).
4. ОБЯЗАТЕЛЬНО посмотри оба кадра Read и оцени глазами владельца.
5. Повторяй 2–4, пока: линтер чист, ✗ нет, ПК масштаб ≥ 0.95, висячих слов по смыслу нет, на кадрах
   не к чему придраться. Обычно 3–6 кругов.

${CANON}

НЕЛЬЗЯ: трогать другие файлы, кроме своего out/slide-${pad(id)}.html; менять заголовок; добавлять
<style>/<script>/<svg>/style= (кроме разрешённого).`

const CRITIC = (id) => `Ты — придирчивый приёмщик слайда ${id} лекции 3, смотришь глазами владельца курса, который
«очень трепетно относится к переносам», ругается на мелкий масштаб, рубрики, цифры в кружках, жирное в
плашках, неровное выравнивание и на несоответствие тексту озвучки. Ты сам исправляешь.

ФАЙЛ: ${R}/build/l3/out/slide-${pad(id)}.html
ЗАДАНИЕ: ${R}/build/l3/specs/slide-${pad(id)}.md (заголовок, «главное», текст озвучки)
КОНТРАКТ: ${R}/build/l3/CONTRACT.md

ПОРЯДОК:
1. cd ${R} && python3 build/l3/probe.py ${id} — затем Read кадров ${R}/build/l3/frames/slide-${pad(id)}-pc.png и -mob.png.
2. Выпиши ВСЕ дефекты как владелец («"…" — перенос», «"…" — в одну строку», «масштаб мелкий»,
   «по центру выровни», «убери "…"», «не "…", а "…"»). Строго.
3. Сверь с текстом озвучки: каждый пункт «главного» виден и сформулирован по смыслу текста? Заголовок
   дословный? Подзаголовок одной строкой, не повторяет вывод? Термины владельца? Анимации есть?
4. Исправь всё (Edit/Write), lint и probe снова, снова кадры. Пока дефектов нет.
   Линтер: cd ${R} && python3 build/l3/lint.py build/l3/out/slide-${pad(id)}.html

${CANON}

НЕЛЬЗЯ: трогать другие файлы; менять заголовок. В ответе честно: найденные дефекты и что осталось.`

const REVIEW = (id) => `Ты — владелец курса и принимаешь слайд ${id} лекции 3 ПЕРЕД лекцией. Слайд уже сверстан и
проверен, но владелец всегда находит ещё что-то. Найди то, что пропустили, и исправь сам, ничего не сломав.

ФАЙЛ: ${R}/build/l3/out/slide-${pad(id)}.html
ЗАДАНИЕ: ${R}/build/l3/specs/slide-${pad(id)}.md ; КОНТРАКТ: ${R}/build/l3/CONTRACT.md

1. cd ${R} && python3 build/l3/probe.py ${id} --light
   Read все четыре кадра: ${R}/build/l3/frames/slide-${pad(id)}-pc.png, -mob.png, -pc-light.png, -mob-light.png.
2. Особенно: висячие слова и переносы не по смыслу; что можно в одну строку; блоки длиннее двух строк;
   рубрики и пояснения, без которых схема понятна; вывод повторяет подзаголовок; параллельные карточки
   разные; иконка оторвана; стрелка не по центру; в светлой теме что-то пропадает (белое на белом,
   бледные заливки, иконка цвета подложки); «главное» из задания видно целиком; соответствие тексту.
3. Исправь (Edit), lint и probe снова, кадры снова. Если правка уронила ПК масштаб ниже 0.95 или кегль
   телефона ниже 10px — откати её. Линтер: cd ${R} && python3 build/l3/lint.py build/l3/out/slide-${pad(id)}.html

${CANON}

НЕЛЬЗЯ: трогать другие файлы; менять заголовок; менять схему целиком. Нет претензий — так и скажи.`

const A_SCHEMA = {
  type: 'object',
  properties: {
    slide: { type: 'number' }, lint_clean: { type: 'boolean' }, probe_clean: { type: 'boolean' },
    pc_scale: { type: 'number' }, mob_kegl: { type: 'number' }, subtitle: { type: 'string' }, summary: { type: 'string' },
  },
  required: ['slide', 'lint_clean', 'probe_clean', 'pc_scale', 'mob_kegl', 'subtitle', 'summary'],
}
const C_SCHEMA = {
  type: 'object',
  properties: {
    slide: { type: 'number' }, defects_found: { type: 'array', items: { type: 'string' } },
    remaining: { type: 'array', items: { type: 'string' } }, covers_all: { type: 'boolean' },
    lint_clean: { type: 'boolean' }, probe_clean: { type: 'boolean' }, pc_scale: { type: 'number' }, mob_kegl: { type: 'number' },
  },
  required: ['slide', 'defects_found', 'remaining', 'covers_all', 'lint_clean', 'probe_clean', 'pc_scale', 'mob_kegl'],
}

const res = await pipeline(
  IDS,
  (id) => agent(AUTHOR(id), { label: `вёрстка:${id}`, phase: 'Вёрстка', schema: A_SCHEMA }),
  (a, id) => agent(CRITIC(id), { label: `приёмка:${id}`, phase: 'Приёмка', schema: C_SCHEMA }).then(c => ({ a, c })),
  (ac, id) => agent(REVIEW(id), { label: `ревизия:${id}`, phase: 'Ревизия', schema: C_SCHEMA }).then(r => ({ ...ac, r })),
)
return res.filter(Boolean).map(x => ({
  slide: x.a && x.a.slide, subtitle: x.a && x.a.subtitle,
  scale: x.r && x.r.pc_scale, kegl: x.r && x.r.mob_kegl, covers: x.c && x.c.covers_all,
  remaining: [...((x.c && x.c.remaining) || []), ...((x.r && x.r.remaining) || [])],
}))
